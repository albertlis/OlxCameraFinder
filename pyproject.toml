[project]
name = "olx-camera-finder"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40.0",
    "httpx>=0.27.0",
    "beautifulsoup4>=4.12.0",
    "openai>=1.0.0",
    "pydantic>=2.0.0",
    "mcp>=1.0.0",
]

# Setup:
#   uv sync
#   uv run playwright install chromium
